# apiVersion: v1
# kind: Service
# metadata:
#   name: backend-feed
# spec:
#   selector:
#     app: backend-feed
#   ports:
#     - protocol: TCP
#       port: 8080
#       targetPort: 8080


 apiVersion: v1
kind: Service
metadata:
  labels:
    service: udagram-api-feed
  name: udagram-api-feed
spec:
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
  selector:
    service: udagram-api-feed